# CONTRIBUTING

サイトの誤字や情報の修正などを歓迎します！
貢献するには、以下の手順に従ってください。

## コード変更前の準備

1. リポジトリをフォークし、ローカルにクローンします。
2. ブランチを作成します。

## 開発環境の準備

```bash
npm install
```

## 動作確認

変更を行ったら、以下を必ず実行してください。

1. サイトが正常に起動するか確認:
```bash
npm run dev
```
ローカルブラウザで `http://localhost:4321` を開き、画面に崩れやエラーがないことを確認します。

2. ビルドが通るか確認:
```bash
npm run build
```
ビルドがエラーなく完了することを確認します。

## 変更内容のコミット

- コミットメッセージは日本語もしくは英語で簡潔に書いてください。
- 例: `fix: ヘッダーのロゴ位置を右に調整`

## Pull Request 作成

- PRタイトルは日本語もしくは英語でわかりやすく
- PR本文に以下を含める
  - 何を変更したか
  - なぜ必要か
  - 動作・ビルド確認結果（`npm run dev` と `npm run build` が成功したか）
